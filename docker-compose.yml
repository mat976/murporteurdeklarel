version: '3.8'

services:
  murporteurdeklarel:
    build: .
    container_name: klarel-mur-porteur
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true
